# generated 2001/2/2 8:41:25 CET by thoma@ig23.
# using glademm V0.5_11c

AC_INIT(src/midgard.cc)
AM_INIT_AUTOMAKE(midgard, 0.4delta0)
AM_CONFIG_HEADER(config.h)

AC_ISC_POSIX
AC_PROG_CC
AM_PROG_CC_STDC
AC_HEADER_STDC
AC_PROG_CPP
AC_PROG_CXX
AC_PROG_CXXCPP
AM_PROG_LIBTOOL

AC_LANG_CPLUSPLUS

PETIG_CHECK_KOMPONENTEN

AC_ARG_ENABLE(utf8, [  --enable-utf8	Recode to UTF8],
	[case "${enableval}" in
	  yes) utf8=true ;;
	  no)  utf8=false ;;
	  *) AC_MSG_ERROR(bad value ${enableval} for --enable-utf8) ;; 
	 esac],[if echo __MINGW32__ | cpp | fgrep -q __MINGW32__
	 	then utf8=false
	 	else utf8=true
	 	fi])
AM_CONDITIONAL(UTF8, test x$utf8 = xtrue)

dnl Set PACKAGE_DATA_DIR in config.h.
if test "x${prefix}" = "xNONE"; then
  PACKAGE_DATA_DIR="${ac_default_prefix}/share/${PACKAGE}/"
else
  PACKAGE_DATA_DIR="${prefix}/share/${PACKAGE}/"
fi
AC_SUBST(PACKAGE_DATA_DIR)

AC_OUTPUT(Makefile src/Makefile src/document_eingabe.tex pixmaps/Makefile \
	xml/Makefile src/document_eingabe4.tex )
