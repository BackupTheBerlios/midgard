// generated 2002/2/9 17:08:28 CET by thoma@Tiger.
// using glademm V0.6.2c_cvs
//
// DO NOT EDIT THIS FILE ! It was created using
// glade-- /home/thoma/rollenspiel/midgard/programme/gcc_3.0/midgard/midgard.glade
// for gtk 1.2.10 and gtkmm 1.2.5
//
// Please modify the corresponding derived classes in ./src/WindowInfo.cc

#include "config.h"
#include "WindowInfo.hh"
#include <gtk--/pixmap.h>
#include "../pixmaps/Gross_ark_pre.xpm"
#include <gtk--/viewport.h>
#include <gtk--/scrolledwindow.h>
#include <gtk--/table.h>
#include <gtk--/box.h>

WindowInfo_glade::WindowInfo_glade(
) : Gtk::Window(GTK_WINDOW_DIALOG)
{   
   
   Gtk::Window *WindowInfo = this;
   Gtk::Pixmap *pixmap54 = manage(new class Gtk::Pixmap(Gross_ark_pre_xpm));
   LogWin = new class logwin();
   
   Gtk::Viewport *viewport37 = manage(new class Gtk::Viewport());
   Gtk::ScrolledWindow *scrolledwindow75 = manage(new class Gtk::ScrolledWindow());
   button_info_ok = manage(new class Gtk::Button("OK"));
   button_bestaetigen = manage(new class Gtk::Button("Bestätigen"));
   button_abbrechen = manage(new class Gtk::Button("Abbrechen"));
   
   Gtk::Table *table125 = manage(new class Gtk::Table(1, 3, false));
   Gtk::Table *table69 = manage(new class Gtk::Table(2, 2, false));
   Gtk::VBox *vbox4 = manage(new class Gtk::VBox(false, 0));
   pixmap54->set_alignment(0.5, 0.5);
   pixmap54->set_padding(0, 0);
   viewport37->add(*LogWin);
   scrolledwindow75->set_usize(400, -1);
   scrolledwindow75->set_policy(GTK_POLICY_AUTOMATIC, GTK_POLICY_AUTOMATIC);
   scrolledwindow75->add(*viewport37);
   button_info_ok->set_usize(50, -1);
   button_info_ok->set_flags(GTK_CAN_FOCUS);
   button_bestaetigen->set_flags(GTK_CAN_FOCUS);
   button_abbrechen->set_flags(GTK_CAN_FOCUS);
   table125->attach(*button_info_ok, 2, 3, 0, 1, GTK_FILL, 0, 0, 0);
   table125->attach(*button_bestaetigen, 0, 1, 0, 1, GTK_FILL, 0, 0, 0);
   table125->attach(*button_abbrechen, 1, 2, 0, 1, 0, 0, 0, 0);
   table69->attach(*pixmap54, 1, 2, 0, 2, 0, 0, 0, 0);
   table69->attach(*scrolledwindow75, 0, 1, 0, 1, GTK_EXPAND|GTK_SHRINK|GTK_FILL, GTK_EXPAND|GTK_SHRINK|GTK_FILL, 0, 0);
   table69->attach(*table125, 0, 1, 1, 2, 0, 0, 0, 0);
   vbox4->pack_start(*table69);
   WindowInfo->set_title("Info");
   WindowInfo->set_default_size(366, 146);
   WindowInfo->add(*vbox4);
   pixmap54->show();
   LogWin->show();
   viewport37->show();
   scrolledwindow75->show();
   button_info_ok->show();
   button_bestaetigen->show();
   button_abbrechen->show();
   table125->show();
   table69->show();
   vbox4->show();
   WindowInfo->show();
   button_info_ok->clicked.connect(SigC::slot(static_cast<class WindowInfo*>(this), &WindowInfo::on_button_info_ok_clicked));
   button_bestaetigen->clicked.connect(SigC::slot(static_cast<class WindowInfo*>(this), &WindowInfo::on_button_bestaetigen_clicked));
   button_abbrechen->clicked.connect(SigC::slot(static_cast<class WindowInfo*>(this), &WindowInfo::on_button_info_ok_clicked));
}

WindowInfo_glade::~WindowInfo_glade()
{   delete LogWin;
}
