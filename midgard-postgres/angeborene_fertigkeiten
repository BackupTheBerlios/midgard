--
-- Selected TOC Entries:
--
DROP TABLE angeborene_fertigkeiten;
--
-- TOC Entry ID 2 (OID 319936)
--
-- Name: angeborene_fertigkeiten Type: TABLE Owner: thoma
--

CREATE TABLE angeborene_fertigkeiten (
	min integer,
	max integer,
	name text,
	wert integer
);

--
-- Data for TOC Entry ID 3 (OID 319936) TABLE DATA angeborene_fertigkeiten
--

-- Disable triggers
UPDATE "pg_class" SET "reltriggers" = 0 WHERE "relname" ~* 'angeborene_fertigkeiten';
COPY angeborene_fertigkeiten  FROM stdin;
86	87	Beidhändigkeit	\N
88	89	Berserkergang	\N
90	91	Richtungssinn	12
92	93	Gefahr spüren	1
94	95	Horchen	5
96	97	Nachtsicht	\N
98	99	Wachgabe	\N
\.
-- Enable triggers
BEGIN TRANSACTION;
CREATE TEMP TABLE "tr" ("tmp_relname" name, "tmp_reltriggers" smallint);
INSERT INTO "tr" SELECT C."relname", count(T."oid") FROM "pg_class" C, "pg_trigger" T WHERE C."oid" = T."tgrelid" AND C."relname" ~* 'angeborene_fertigkeiten'  GROUP BY 1;
UPDATE "pg_class" SET "reltriggers" = TMP."tmp_reltriggers" FROM "tr" TMP WHERE "pg_class"."relname" = TMP."tmp_relname";
DROP TABLE "tr";
COMMIT TRANSACTION;

