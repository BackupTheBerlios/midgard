--
-- Selected TOC Entries:
--
DROP TABLE "zauber_beschreibung";
--
-- TOC Entry ID 2 (OID 367686)
--
-- Name: zauber_beschreibung Type: TABLE Owner: thoma
--

CREATE TABLE "zauber_beschreibung" (
	"name" text NOT NULL,
	"beschreibung" text
);

--
-- Data for TOC Entry ID 3 (OID 367686) TABLE DATA zauber_beschreibung
--

-- Disable triggers
UPDATE "pg_class" SET "reltriggers" = 0 WHERE "relname" ~* 'zauber_beschreibung';
COPY "zauber_beschreibung"  FROM stdin;
Heilen von Wunden	Heilt W6 LP und AP, nur alle drei Tage möglich
\.
-- Enable triggers
BEGIN TRANSACTION;
CREATE TEMP TABLE "tr" ("tmp_relname" name, "tmp_reltriggers" smallint);
INSERT INTO "tr" SELECT C."relname", count(T."oid") FROM "pg_class" C, "pg_trigger" T WHERE C."oid" = T."tgrelid" AND C."relname" ~* 'zauber_beschreibung'  GROUP BY 1;
UPDATE "pg_class" SET "reltriggers" = TMP."tmp_reltriggers" FROM "tr" TMP WHERE "pg_class"."relname" = TMP."tmp_relname";
DROP TABLE "tr";
COMMIT TRANSACTION;

